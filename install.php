<?php

   $action = mysql_real_escape_string($_GET["action"]);

// write settings into dbconnectivity.php

// SET UP DATABASE:
// create table numbers (ix bigint not null primary key, provider INT NOT NULL DEFAULT -1, lastcheck DATETIME, country BIGINT);
// create table provider (ix INT NOT NULL, name VARCHAR(40));


?>
